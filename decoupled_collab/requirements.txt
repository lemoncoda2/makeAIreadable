# Decoupled Collaboration — pins for Qwen3-4B on 4×V100-32G (sm_70)
#
# Install PyTorch FIRST (pick one):
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   # or: pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#
# Then: pip install -r requirements.txt
#
# Compatibility notes (research snapshot):
# - Qwen3 requires transformers>=4.51.0 (else KeyError: qwen3 / no enable_thinking)
# - Qwen docs recommend vllm>=0.8.5 for Qwen3 serving
# - V100 (sm_70): vLLM 0.9+ prebuilt images commonly drop CC<8.0; pin vllm==0.8.5
# - Use FP16 only on V100 (no bf16). Prefer HF+PEFT for training; vLLM optional.
# - If vllm 0.8.5 wheel fails on your driver, omit vllm and keep inference.use_vllm=false

transformers>=4.51.0,<4.53
trl==0.15.2
peft==0.14.0
# Transformers 4.52 calls Accelerator.unwrap_model(keep_torch_compile=...).
# That public argument is present in Accelerate 1.6.0, but not 1.2.1.
accelerate==1.6.0
datasets==3.2.0
openai>=1.40.0
evalplus
wandb
jsonlines
rich
pyyaml>=6.0
tqdm
pytest>=7.0
huggingface_hub
# uv-created virtualenvs do not seed setuptools; keep it available for legacy
# runtime imports and optional native-extension builds.
setuptools>=74.1.1,<81

# Optional inference accelerator (Qwen3 + V100 intersection):
# vllm==0.8.5
# Optional training engine (the default Accelerate/DDP configs do not use it):
# deepspeed==0.15.4
